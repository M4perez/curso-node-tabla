# Notas
Este es mi primer programa en node.

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Base de multiplicar                                    [number]
  -l, --listar   Listar Tablas                        [boolean] [default: false]
  -h, --hasta    hasta donde tiene que llegar la tabla    [number] [default: 10]
```